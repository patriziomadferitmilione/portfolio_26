# Repository Instructions

## Safety Rules

- Never remove code unless the user specifically requests it.
- Even when the user requests removal, ask for confirmation before deleting anything.
- Never run a build command.
- Even when explicitly instructed to run a build command, ask for confirmation first.
